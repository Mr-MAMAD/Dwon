name: Pornhub Downloader

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: "Pornhub Video URL(s) - comma, space or newline separated"
        required: true
        type: string

      quality:
        description: "Video Quality"
        required: true
        default: "720"
        type: choice
        options:
          - "240"
          - "360"
          - "480"
          - "720"
          - "1080"
          - "best"

jobs:
  download:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install tools
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y ffmpeg
          python3 -m pip install -U yt-dlp

      - name: Prepare folders
        run: |
          rm -rf downloads
          mkdir -p downloads

      - name: Download videos (FAIL SAFE + REPORT)
        shell: bash
        run: |
          set +e

          RAW_URLS="${{ github.event.inputs.video_url }}"
          QUALITY="${{ github.event.inputs.quality }}"

          if [ "$QUALITY" = "best" ]; then
            FORMAT="bestvideo+bestaudio/best"
          else
            FORMAT="bestvideo[height<=${QUALITY}]+bestaudio/best[height<=${QUALITY}]"
          fi

          CLEAN_URLS=$(echo "$RAW_URLS" | tr ',' '\n' | tr ' ' '\n')

          SUCCESS_FILE="downloads/success.txt"
          FAILED_FILE="downloads/failed.txt"

          > "$SUCCESS_FILE"
          > "$FAILED_FILE"

          INDEX=1

          for URL in $CLEAN_URLS; do
            URL=$(echo "$URL" | xargs)

            if [ -z "$URL" ]; then
              continue
            fi

            echo "======================================"
            echo "Downloading [$INDEX]"
            echo "$URL"
            echo "======================================"

            yt-dlp \
              --no-playlist \
              --ignore-errors \
              --no-warnings \
              --user-agent "Mozilla/5.0 (Windows NT 10.0; Win64; x64) Chrome/124 Safari/537.36" \
              --add-header "Referer:https://www.pornhub.com/" \
              -f "$FORMAT" \
              --merge-output-format mp4 \
              -o "downloads/%(title).150s_${INDEX}.%(ext)s" \
              "$URL"

            STATUS=$?

            if [ $STATUS -ne 0 ]; then
              echo "FAILED: $URL"
              echo "$URL" >> "$FAILED_FILE"
            else
              echo "SUCCESS: $URL"
              echo "$URL" >> "$SUCCESS_FILE"
            fi

            INDEX=$((INDEX + 1))
          done

          set -e

      - name: Remove invalid files
        shell: bash
        run: |
          find downloads -type f -empty -delete

      - name: Check downloaded files
        id: check_files
        shell: bash
        run: |
          COUNT=$(find downloads -type f ! -name "success.txt" ! -name "failed.txt" | wc -l)

          echo "count=$COUNT" >> "$GITHUB_OUTPUT"

          if [ "$COUNT" -eq 0 ]; then
            echo "No valid downloaded files found."
          fi

      - name: Generate Release Info
        id: meta
        shell: bash
        run: |
          TAG="run-$(date +%Y%m%d-%H%M%S)"

          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

          echo "## Download Report" > body.md
          echo "" >> body.md

          echo "### Successful Downloads" >> body.md

          if [ -f downloads/success.txt ]; then
            while read line; do
              echo "- $line" >> body.md
            done < downloads/success.txt
          fi

          echo "" >> body.md

          echo "### Failed Downloads" >> body.md

          if [ -f downloads/failed.txt ]; then
            while read line; do
              echo "- $line" >> body.md
            done < downloads/failed.txt
          fi

      - name: Create Release
        if: steps.check_files.outputs.count != '0'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.meta.outputs.tag }}
          name: Download ${{ steps.meta.outputs.tag }}
          body_path: body.md
          files: |
            downloads/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
