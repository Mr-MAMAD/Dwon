name: SxyPrn Video Downloader

on:
  workflow_dispatch:
    inputs:
      urls:
        description: 'لینک صفحات SxyPrn (هر خط یک لینک)'
        required: true
        type: string
      quality:
        description: 'کیفیت (max/hd/sd)'
        required: false
        default: 'max'
        type: choice
        options:
          - max
          - hd
          - sd

jobs:
  download-videos:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install tools
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq curl wget aria2 jq pup

      - name: Download videos
        id: download
        run: |
          mapfile -t URL_ARRAY <<< "${{ github.event.inputs.urls }}"
          QUALITY="${{ github.event.inputs.quality }}"
          
          SUCCESS_COUNT=0
          FAILED_LIST=""
          TOTAL_SIZE=0
          
          mkdir -p downloads
          
          INDEX=0
          for page_url in "${URL_ARRAY[@]}"; do
            page_url=$(echo "$page_url" | xargs)
            if [ -z "$page_url" ]; then
              continue
            fi
            
            INDEX=$((INDEX + 1))
            
            echo "=========================================="
            echo "[$INDEX] 🌐 صفحه: $page_url"
            echo "=========================================="
            
            # استخراج slug از URL برای نام فایل
            SLUG=$(echo "$page_url" | grep -oP 'post/\K[^?]+' | sed 's/\.html$//')
            if [ -z "$SLUG" ]; then
              SLUG="video_$INDEX"
            fi
            
            # دانلود صفحه
            echo "📥 دانلود صفحه..."
            PAGE_HTML=$(curl -s -L \
              -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" \
              -H "Accept: text/html,application/xhtml+xml,application/xml;q=0.9,*/*;q=0.8" \
              -H "Accept-Language: en-US,en;q=0.5" \
              --connect-timeout 30 \
              --max-time 60 \
              "$page_url" 2>/dev/null)
            
            if [ -z "$PAGE_HTML" ]; then
              echo "❌ صفحه دانلود نشد"
              FAILED_LIST="$FAILED_LIST\n- [$INDEX] $page_url (صفحه لود نشد)"
              continue
            fi
            
            echo "✅ صفحه دانلود شد ($(echo "$PAGE_HTML" | wc -c) بایت)"
            
            # روش 1: استخراج لینک از video source
            echo "🔍 جستجوی لینک ویدیو..."
            
            # استخراج همه لینک‌های احتمالی ویدیو
            VIDEO_URL=""
            
            # روش 1a: src در تگ video
            VIDEO_URL=$(echo "$PAGE_HTML" | grep -oP 'src="https?://[^"]*trafficdeposit[^"]*' | head -1 | sed 's/^src="//')
            
            # روش 1b: data-src
            if [ -z "$VIDEO_URL" ]; then
              VIDEO_URL=$(echo "$PAGE_HTML" | grep -oP 'data-src="https?://[^"]*trafficdeposit[^"]*' | head -1 | sed 's/^data-src="//')
            fi
            
            # روش 1c: video_url در جاوااسکریپت
            if [ -z "$VIDEO_URL" ]; then
              VIDEO_URL=$(echo "$PAGE_HTML" | grep -oP "video_url:\s*'https?://[^']*trafficdeposit[^']*" | head -1 | sed "s/video_url:\s*'//")
            fi
            
            # روش 1d: source داخل video
            if [ -z "$VIDEO_URL" ]; then
              VIDEO_URL=$(echo "$PAGE_HTML" | grep -oP '<source[^>]*src="https?://[^"]*trafficdeposit[^"]*' | head -1 | grep -oP 'src="\K[^"]*')
            fi
            
            # روش 1e: هر لینکی که trafficdeposit داشته باشه
            if [ -z "$VIDEO_URL" ]; then
              VIDEO_URL=$(echo "$PAGE_HTML" | grep -oP 'https?://[^"'\''<>]*trafficdeposit[^"'\''<>]*\.(mp4|vid|m3u8|ts)[^"'\''<>]*' | head -1)
            fi
            
            # روش 1f: لینک‌های widi
            if [ -z "$VIDEO_URL" ]; then
              VIDEO_URL=$(echo "$PAGE_HTML" | grep -oP 'https?://[^"'\''<>]*/widi/[^"'\''<>]*' | head -1)
            fi
            
            # روش 2: استخراج از iframe
            if [ -z "$VIDEO_URL" ]; then
              echo "🔍 بررسی iframe..."
              IFRAME_URL=$(echo "$PAGE_HTML" | grep -oP 'iframe[^>]*src="https?://[^"]*"' | head -1 | grep -oP 'src="\K[^"]*')
              
              if [ -n "$IFRAME_URL" ]; then
                echo "📥 دانلود iframe: ${IFRAME_URL:0:100}..."
                IFRAME_HTML=$(curl -s -L \
                  -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36" \
                  -H "Referer: $page_url" \
                  --connect-timeout 30 \
                  --max-time 60 \
                  "$IFRAME_URL" 2>/dev/null)
                
                VIDEO_URL=$(echo "$IFRAME_HTML" | grep -oP 'https?://[^"'\''<>]*trafficdeposit[^"'\''<>]*\.(mp4|vid)[^"'\''<>]*' | head -1)
              fi
            fi
            
            # روش 3: API endpoint
            if [ -z "$VIDEO_URL" ]; then
              echo "🔍 بررسی API..."
              # استخراج video ID
              VIDEO_ID=$(echo "$page_url" | grep -oP 'post/\K[^.?]+')
              API_URL="https://sxyprn.com/api/video/$VIDEO_ID"
              
              API_RESPONSE=$(curl -s -L \
                -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36" \
                -H "Accept: application/json" \
                -H "Referer: $page_url" \
                --connect-timeout 30 \
                --max-time 60 \
                "$API_URL" 2>/dev/null)
              
              VIDEO_URL=$(echo "$API_RESPONSE" | grep -oP '"video_url":"[^"]*"' | cut -d'"' -f4 | sed 's/\\//g')
              [ -z "$VIDEO_URL" ] && VIDEO_URL=$(echo "$API_RESPONSE" | grep -oP '"file":"[^"]*"' | cut -d'"' -f4)
            fi
            
            # روش 4: کل HTML برای هر لینک trafficdeposit
            if [ -z "$VIDEO_URL" ]; then
              echo "🔍 جستجوی عمیق..."
              # ذخیره HTML برای دیباگ
              echo "$PAGE_HTML" > "/tmp/page_$INDEX.html"
              
              # استخراج همه لینک‌ها
              ALL_LINKS=$(echo "$PAGE_HTML" | grep -oP 'https?://[^"'\''<>\s]+' | sort -u)
              VIDEO_URL=$(echo "$ALL_LINKS" | grep -i "trafficdeposit" | head -1)
              [ -z "$VIDEO_URL" ] && VIDEO_URL=$(echo "$ALL_LINKS" | grep -i "\.vid\|\.mp4\|/widi/" | head -1)
            fi
            
            if [ -z "$VIDEO_URL" ]; then
              echo "❌ لینک ویدیو پیدا نشد"
              echo "📄 بخشی از صفحه:"
              echo "$PAGE_HTML" | grep -i "video\|source\|trafficdeposit\|widi\|\.vid\|\.mp4" | head -10
              FAILED_LIST="$FAILED_LIST\n- [$INDEX] $page_url (لینک ویدیو استخراج نشد)"
              continue
            fi
            
            echo "🎬 لینک ویدیو: ${VIDEO_URL:0:120}..."
            
            # تعیین نام فایل
            FILENAME="${SLUG}.mp4"
            
            echo "📁 ذخیره به عنوان: $FILENAME"
            
            # ساخت هدرها
            HEADER_OPTS=(
              --header="User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
              --header="Accept: */*"
              --header="Accept-Language: en-US,en;q=0.9"
              --header="Accept-Encoding: identity"
              --header="Connection: keep-alive"
              --header="Cache-Control: no-cache"
              --header="Sec-Fetch-Dest: video"
              --header="Sec-Fetch-Mode: no-cors"
              --header="Sec-Fetch-Site: cross-site"
              --referer="$page_url"
              --header="Referer: $page_url"
              --header="Origin: https://sxyprn.com"
            )
            
            echo "🚀 دانلود ویدیو..."
            
            # دانلود با aria2
            aria2c \
              "$VIDEO_URL" \
              --out="downloads/$FILENAME" \
              --max-connection-per-server=8 \
              --split=8 \
              --min-split-size=1M \
              --connect-timeout=30 \
              --timeout=600 \
              --max-tries=3 \
              --retry-wait=5 \
              --continue=true \
              --allow-overwrite=false \
              --auto-file-renaming=false \
              --file-allocation=none \
              --disk-cache=64M \
              --summary-interval=5 \
              --console-log-level=notice \
              "${HEADER_OPTS[@]}" \
              2>&1 | tee "/tmp/aria2_${INDEX}.log"
            
            ARIA_EXIT=${PIPESTATUS[0]}
            
            # Fallback to curl
            if [ $ARIA_EXIT -ne 0 ]; then
              echo "⚠️ تلاش با curl..."
              curl -L \
                -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36" \
                -H "Referer: $page_url" \
                -H "Origin: https://sxyprn.com" \
                --connect-timeout 30 \
                --max-time 600 \
                -o "downloads/$FILENAME" \
                "$VIDEO_URL" 2>&1
            fi
            
            # بررسی نتیجه
            if [ -f "downloads/$FILENAME" ]; then
              SIZE=$(stat -c%s "downloads/$FILENAME" 2>/dev/null || echo 0)
              if [ "$SIZE" -gt 102400 ]; then
                echo "✅ موفق: $FILENAME ($(numfmt --to=iec $SIZE))"
                SUCCESS_COUNT=$((SUCCESS_COUNT + 1))
                TOTAL_SIZE=$((TOTAL_SIZE + SIZE))
              else
                echo "❌ حجم کم: $SIZE بایت"
                rm -f "downloads/$FILENAME"
                FAILED_LIST="$FAILED_LIST\n- [$INDEX] $page_url (حجم غیرعادی)"
              fi
            else
              echo "❌ دانلود ناموفق"
              FAILED_LIST="$FAILED_LIST\n- [$INDEX] $page_url (خطا در دانلود)"
            fi
            
            echo ""
          done
          
          echo "SUCCESS_COUNT=$SUCCESS_COUNT" >> $GITHUB_OUTPUT
          echo "TOTAL_SIZE=$TOTAL_SIZE" >> $GITHUB_OUTPUT
          
          # ذخیره HTML صفحات برای دیباگ
          tar czf /tmp/pages_html.tar.gz /tmp/page_*.html 2>/dev/null || true
          
          {
            echo "# 📊 گزارش دانلود SxyPrn"
            echo ""
            echo "| مورد | مقدار |"
            echo "|------|-------|"
            echo "| کل صفحات | $INDEX |"
            echo "| دانلود موفق | $SUCCESS_COUNT |"
            echo "| حجم کل | $(numfmt --to=iec $TOTAL_SIZE) |"
            echo ""
            
            if [ $SUCCESS_COUNT -gt 0 ]; then
              echo "## ✅ ویدیوهای دانلود شده"
              echo ""
              for f in downloads/*; do
                [ -f "$f" ] && echo "- 🎬 \`$(basename "$f")\` ($(numfmt --to=iec $(stat -c%s "$f")))"
              done
            fi
            
            if [ -n "$FAILED_LIST" ]; then
              echo ""
              echo "## ❌ خطاها"
              echo -e "$FAILED_LIST"
            fi
          } > download_report.md
          
          if [ "$SUCCESS_COUNT" -eq 0 ]; then
            echo "❌ هیچ ویدیویی دانلود نشد!"
            exit 1
          fi

      - name: Create Release
        if: steps.download.outputs.SUCCESS_COUNT != '0'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: sxyprn-${{ github.run_number }}
          name: 🎬 SxyPrn Videos #${{ github.run_number }}
          body_path: download_report.md
          files: |
            downloads/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload debug data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-data-${{ github.run_number }}
          path: |
            /tmp/pages_html.tar.gz
            /tmp/aria2_*.log
            download_report.md
          retention-days: 3
